# 빌드 산출물 · 로컬 캐시
build/
.gradle/

# VCS · IDE · 문서
.git/
.github/
.idea/
.vscode/
.DS_Store
*.md
docs/

# 테스트 소스 (이미지 빌드 시 -x test 이므로 불필요)
src/test/

# 로컬 개발 설정 — localhost 접속 정보라 컨테이너 안에서는 어차피 안 맞고,
# 개발자 개인 설정 성격이 강해 이미지에 포함하지 않는다
# (dev는 컴포즈로 앱 컨테이너를 띄울 때 필요해 포함한다. prod는 시크릿을 환경변수로 주입받는다)
src/main/resources/application-local.yaml

# 도커 컴포즈 파일 — 이미지에는 불필요
docker-compose.yml
docker-compose.dev.yml
